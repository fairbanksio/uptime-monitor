apiVersion: v1
kind: Service
metadata:
  name: {{ include "uptime-web.fullname" . }}
  labels:
    {{- include "uptime-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "uptime-web.selectorLabels" . | nindent 4 }}
