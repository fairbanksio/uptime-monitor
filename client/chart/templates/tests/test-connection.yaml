apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "uptime-web.fullname" . }}-test-connection"
  labels:
{{ include "uptime-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "uptime-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
